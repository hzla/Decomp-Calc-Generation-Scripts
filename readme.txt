Requirements:

Install latest version of ruby

'calc' folder is to be placed in the root pokeemerald-expansion directory 

All parse scripts are to be run inside the `calc` folder

You must build your rom at least once so that `src/data/trainer_parties.h` is generated from the trainers.party file
